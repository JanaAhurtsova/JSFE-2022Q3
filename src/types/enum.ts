const enum PageIds {
  GARAGE = 'garage',
  WINNERS = 'winners',
}

const enum SortWinners {
  TIMES = 'times',
  WINS = 'wins',
}

const enum Sort {
  ID = 'id',
  TIME = 'time',
  WINS = 'wins',
}

const enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export { PageIds, SortWinners, Sort, Order };
