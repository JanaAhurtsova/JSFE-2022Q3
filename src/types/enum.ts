const enum PageIds {
  GARAGE = 'garage',
  WINNERS = 'winners',
}

const enum SortWinners {
  WINS = 'wins',
  TIMES = 'times',
}

const enum Sort {
  ID = 'id',
  WINS = 'wins',
  TIME = 'time',
}

const enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export { PageIds, SortWinners, Sort, Order };
